# Make sure the Maven wrapper shell script always uses LF (for bash)
mvnw text eol=lf
*.sh text eol=lf

# Windows batch files should use CRLF
*.cmd text eol=crlf
*.bat text eol=crlf

# Source code and text files get normalized
* text=auto

# Optional: Treat binary files safely
*.png binary
*.jpg binary
*.pdf binary